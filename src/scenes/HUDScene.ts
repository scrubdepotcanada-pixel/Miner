import Phaser from 'phaser';

// Full HUD overlay scene — wired up in Session 3.
// For now, GameScene renders its own inline HUD.
export class HUDScene extends Phaser.Scene {
  constructor() {
    super({ key: 'HUDScene' });
  }

  create(): void {
    // Placeholder — implemented in Session 3
  }
}
